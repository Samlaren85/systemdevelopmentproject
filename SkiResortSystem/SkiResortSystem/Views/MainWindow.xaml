<Window x:Class="SkiResortSystem.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SkiResortSystem.ViewModels"
        xmlns:comp="clr-namespace:SkiResortSystem.Components"
        xmlns:local="clr-namespace:SkiResortSystem.Views"
        mc:Ignorable="d"
        Title="{Binding LoggedInUser, StringFormat=MainWindow ({0})}" Height="auto" Width="1920" FontFamily="Candara" Visibility="{Binding MainVisibility}" Background="#FF5e8ec2" WindowState="Maximized">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <TabControl Background="#FFA6C3E4" Height="1000" Width="auto" BorderBrush="White" BorderThickness="0,2,0,0" Padding="-4,0,0,0" >

        <TabControl.Resources>

            <Style TargetType="TabItem" x:Key="TB1">
                <Setter Property="Background" Value="#FF5e8ec2" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="Height" Value="50" />
                <Setter Property="Width" Value="192" />
                <Setter Property="FontSize" Value="13"/>

                <Setter Property="Template" >
                    <Setter.Value>
                        <ControlTemplate TargetType="TabItem">

                            <Grid>

                                <VisualStateManager.VisualStateGroups>

                                    <VisualStateGroup x:Name="SelectionStates">
                                        <VisualState x:Name="Unselected" />

                                        <VisualState x:Name="Selected">

                                            <Storyboard>

                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="TextBlock"
                                           Storyboard.TargetProperty="TextDecorations">

                                                    <DiscreteObjectKeyFrame KeyTime="0">
                                                        <DiscreteObjectKeyFrame.Value>
                                                            <TextDecorationCollection>
                                                                <TextDecoration Location="Underline" PenThicknessUnit="FontRecommended">
                                                                    <TextDecoration.Pen>

                                                                        <Pen Thickness="7" Brush="White" />
                                                                    </TextDecoration.Pen>
                                                                </TextDecoration>
                                                            </TextDecorationCollection>
                                                        </DiscreteObjectKeyFrame.Value>
                                                    </DiscreteObjectKeyFrame>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>


                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <Border Background="#FF5e8ec2" BorderThickness="0,0,2,0" BorderBrush="White">
                                    <TextBlock x:Name="TextBlock" Text="{TemplateBinding Header}" VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center" />
                                </Border>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>



                </Setter>


            </Style>



            <Style TargetType="TextBox">

                <Setter Property="Height" Value="20"/>
                <Setter Property="FontSize" Value="16"/>

            </Style>

        </TabControl.Resources>



        <TabItem>
            <TabItem.Header >

                <Image Height="100" Width="100" HorizontalAlignment="Center" VerticalAlignment="Center" Source="./images/Logo.png"/>

            </TabItem.Header>

            <TabItem.Style>
                <Style TargetType="TabItem" >
                    <Setter Property="Background" Value="#FF5e8ec2" />
                    <Setter Property="Height" Value="50" />
                    <Setter Property="Width" Value="192" />

                </Style>
            </TabItem.Style>

            <TabItem.Template>
                <ControlTemplate TargetType="TabItem">
                    <Grid>
                        <Border Background="#FF5e8ec2" BorderThickness="0,0,2,0" BorderBrush="White">
                            <ContentPresenter ContentSource="Header" VerticalAlignment="Center" HorizontalAlignment="Center" />
                        </Border>
                    </Grid>
                </ControlTemplate>
            </TabItem.Template>

            <TabControl SelectedItem="{Binding MenuChoise}" TabStripPlacement="Left" Background="#FFA6C3E4">
                <TabItem Header="HuvudMeny" Height="0" IsSelected="True">
                    <Image Source="./images/Logo.png" HorizontalAlignment="Center" VerticalAlignment="Center" Height="500" Width="500" />
                </TabItem>
                <TabItem Header="Logga ut"/>
                <TabItem Header="Avsluta"/>
            </TabControl>
        </TabItem>

        <TabItem Header="BOENDE &amp; KONFERENS" Style="{StaticResource TB1}" >

            <comp:BoendeModulen/>

        </TabItem>

        <TabItem Header="AKTIVITET &amp; UTRUSTNING" Style="{StaticResource TB1}"   >

            <comp:AktivitetsModulen/>

        </TabItem>

        <TabItem Header="KUND" Style="{StaticResource TB1}">

            <comp:KundModulen/>

        </TabItem>

        <TabItem Header="EKONOMI" Style="{StaticResource TB1}"  >

            <comp:EkonomiModulen/>

        </TabItem>

        <TabItem Header="STYRNING" Style="{StaticResource TB1}">

            <comp:StyrningsModulen/>

        </TabItem>

        <TabItem Header="ADMIN" Style="{StaticResource TB1}">

            <comp:AdminModulen/>

        </TabItem>

    </TabControl>





</Window>
